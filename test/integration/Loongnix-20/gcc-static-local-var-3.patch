diff --git a/kernel/reboot.c b/kernel/reboot.c
index 45bea54f9aee..7ef87801bb5c 100644
--- a/kernel/reboot.c
+++ b/kernel/reboot.c
@@ -393,8 +393,15 @@ SYSCALL_DEFINE4(reboot, int, magic1, int, magic2, unsigned int, cmd,
 	return ret;
 }
 
+void kpatch_bar(void)
+{
+	if (!jiffies)
+		printk("kpatch_foo\n");
+}
+
 static void deferred_cad(struct work_struct *dummy)
 {
+	kpatch_bar();
 	kernel_restart(NULL);
 }
 
